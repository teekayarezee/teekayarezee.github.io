//  _____ _        _____
// |_   _| | ___ _|__  /
//   | | | |/ / '__|/ / 
//   | | |   <| |  / /_ 
//   |_| |_|\_\_| /____|
//
// Farming Tick Timer/Clock/Calculator 
// PHP to HTML/JS Re-write
// Many thanks to "Indecent Act" for permission
//
// Version 1.6c
// Original Author: "Indecent Act" / @RSLinkify
// Author:          "TkrZ" / @TkrZ

function countDown() {
    "use strict";
    var e, t = 10,
        o = 6,
        n = new Date(),
        r = n.getUTCHours() < 12 ? n.getUTCHours() + 24 : n.getUTCHours(),
        c = n.getUTCMinutes(),
        u = n.getUTCSeconds(),
        i = 60 * r + c,
        s = function (e, t) {
            document.getElementById(e).innerHTML = t;
        },
        l = ("0" + (60 - n.getUTCSeconds())).slice(-2),
        g = function () {
            for (e = 6; e > 0; e -= 1) {
                for (var n, r, c, g, a = 6400; a >= i;) a -= t;
                n = a + t - i, 1 > n && (n = t), n *= 60, r = Math.floor((n - u) / 3600), c = Math.floor((n - u) % 3600 / 60), g = 1e3 * (3600 * r + 60 * c + (n - u) % 60), g = g > 1e5 * o || 0 > g ? 1e5 * o : g, t *= 2, o *= 2, s("count" + e, ("0" + Math.floor(g / 36e5)).slice(-2) + ":" + ("0" + Math.floor(g % 36e5 / 6e4)).slice(-2) + ":" + l)
            }
        };
    g(), s("currentHour", n.getUTCHours()), s("currentMin", c), s("currentSec", u), s("am_pm", n.getUTCHours() >= 12 ? "PM" : "AM"), s("currentDate", n.toString().split(" ").splice(1, 3).join(" ")), document.getElementById("icon").style.backgroundPosition = "-" + (Math.floor((3600 * r + 60 * c + u) / 30) + -405) + "px 0px"
}
